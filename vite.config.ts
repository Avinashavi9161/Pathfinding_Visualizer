import { defineConfig } from 'vite'

// GitHub Pages serves the site from /<repo-name>/, not /.
// Keep this in sync with your repository name.
export default defineConfig({
  base: '/Pathfinding_Visualizer/',
})

